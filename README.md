# ITX Risk Fusion Engine Demo

This is the updated deploy-ready Next.js version of the executive demo.

## Run locally

```bash
npm install
npm run dev
```

Then open `http://localhost:3000`

## Deploy to Railway

Use:
- Build command: `npm run build`
- Start command: `npm start`
