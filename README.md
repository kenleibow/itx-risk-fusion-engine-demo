# ITX Risk Fusion Engine Demo - Fixed Package

This package uses a simple static site with a tiny Node server and no external dependencies.

## Local run
```bash
npm start
```

Then open http://localhost:3000

## Railway
Railway should deploy this automatically.
If prompted:
- Build command: leave blank
- Start command: npm start
